document.addEventListener("DOMContentLoaded", function(){
  var swiper1 = new Swiper('.swiper-wrapper', {
    // Optional parameters
   slidesPerView: 1,
	 spaceBetween: 2,
	 initialSlide: 1,
    loop: true,
    centeredSlides: true,
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
    },

    // Navigation arrows
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
  })

  var swiper2 = new Swiper('.swiper-wrapper1', {
    // Optional parameters
   slidesPerView: 1,
	 spaceBetween: 2,
	 initialSlide: 1,
    loop: true,
    centeredSlides: true,
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
    },

    // Navigation arrows
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
  })

  var swiper3 = new Swiper('.swiper-wrapper2', {
    // Optional parameters
   slidesPerView: 1,
	 spaceBetween: 2,
	 initialSlide: 1,
    loop: true,
    centeredSlides: true,
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
    },

    // Navigation arrows
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
  })

  var swiper4 = new Swiper('.swiper-wrapper3', {
    // Optional parameters
   slidesPerView: 1,
	 spaceBetween: 2,
	 initialSlide: 1,
    loop: true,
    centeredSlides: true,
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
    },

    // Navigation arrows
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
  })

  var swiper5 = new Swiper('.swiper-wrapper4', {
    // Optional parameters
   slidesPerView: 1,
	 spaceBetween: 2,
	 initialSlide: 1,
    loop: true,
    centeredSlides: true,
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
    },

    // Navigation arrows
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
  })
});
